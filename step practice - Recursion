#include <iostream>

using namespace std;

int factor(int n);

int main(){
    int N;
    cin >> N;
    
    cout << factor(N);
    
    return 0;
}

int factor(int n){
    if(n == 0 || n == 1){
        return 1;
    }
    else{
        return (n * factor(n - 1));
    }
}
