/*#include <iostream>

using namespace std;

int main() {

	int x, y, w, h;
	int result = 0;
	int minX, minY;

	cin >> x >> y >> w >> h;

	minX = (w - x) < x ? (w - x) : x;
	minY = (h - y) < y ? (h - y) : y;
	result = minX < minY ? minX : minY;

	cout << result;
	return 0;
}*/